# List all branches (including remote branches)
git branch -a

# If you see a branch like 'main', use that instead
git push origin main

# If you have a different branch name like 'development', use:
# git push origin development